#include <matioCpp/matioCpp.h>

#include "arr.cc"
#include <cstdio>
int main()
{
	Eigen::Tensor<
	int a = 5, b = 10;
	a = a + b - ( b = a );
	Arr< int > arr( 10 );
	printf( "%d %d\n", a, b );
	scanf( "%d", &a );
}