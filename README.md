# mymlbase
先瞎几把写

还是先把cpu搞懂


cmake也一边去

哎，一堆妥协与仅限二维的操作，因为此项目也用于提交作为机器学习基础实验结果，急急急

现在觉得就算在能够把这个写的到达超出预期的效果（能够复古使用）的假设下还是只能用于模型可行性验证，因为我总觉得是需要特化才能有能接受的性能的

然而既然如此不如从机器码开始特化，懒懒懒

自动微分的设计是

在想要获取对应梯度之前需要构建(build)，这个过程为自然构图，完全没有维护一个整体图，这样做的原因即我认为理论上无关的变量对应的梯度应该约定不会被访问，也即不需要置零，训练只是训练，如果你非要取一个从你ai生成的论文的公式里根本无关的变量的梯度来backward，那很好了

然而这个顾虑大概率只会出现使用高阶梯度的情况，